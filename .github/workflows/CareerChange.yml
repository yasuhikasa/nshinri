name: Post Career Change to X

on:
  # schedule:
  #   - cron: '0 7 */3 * *' # JST 16時、3日に1回
  workflow_dispatch: # 手動トリガー

jobs:
  post-career-change:
    runs-on: ubuntu-latest

    steps:
      - name: Post Career Change Content to X
        run: curl -X POST https://nshinri-56d2c5892705.herokuapp.com/api/post-careerchange
